import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ecomed.aiplantdoctor',
  appName: 'EcoMed - AI Plant Doctor',
  webDir: 'dist'
};

export default config;
